import { defineConfig } from "astro/config"; import sitemap from "@astrojs/sitemap";
export default defineConfig({ site: "https://www.travelstylehub.com", integrations: [sitemap()], trailingSlash: "always" });